# Example SSH config for your Git server (Gerrit, GitHub Enterprise, etc.)
# Adjust Host and connection parameters for your environment.
Host gerrit.your-company.com
    PubkeyAcceptedKeyTypes ssh-rsa,ecdsa-sha2-nistp256
    StrictHostKeyChecking no
    UserKnownHostsFile /dev/null
